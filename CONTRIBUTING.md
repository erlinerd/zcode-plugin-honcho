# Contributing

Run `npm run check` before committing. Keep hook stdout protocol-only, send
logging to stderr, and add tests for every behavior change. Never add secrets or
raw private transcripts to fixtures.
